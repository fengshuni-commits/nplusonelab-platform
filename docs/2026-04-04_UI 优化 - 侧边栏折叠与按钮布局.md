# UI 优化 - 侧边栏折叠与按钮布局

**优化时间**: 2026-04-04 10:55  
**需求来源**: Shuni

---

## 🎨 优化内容

### 1. 操作按钮垂直排列

**优化前**：
```
[BOM 详情] [改价] [库存]  ← 横向排列，占用较宽
```

**优化后**：
```
[BOM 详情]
[改价]     ← 垂直排列，节省空间
[库存]
```

**实现方式**：
- 使用 `flex-direction: column` 垂直布局
- 按钮居中对齐
- 间距 4px
- 表格单元格居中对齐

---

### 2. 侧边栏可折叠

**功能特性**：
- ✅ 点击顶部 **☰** 按钮切换折叠/展开
- ✅ 折叠后宽度：220px → 64px
- ✅ 折叠后只显示图标，隐藏文字
- ✅ 平滑过渡动画（0.3s）
- ✅ 自动保存状态到 localStorage
- ✅ 刷新页面保持折叠状态

**展开状态**：
```
┌─────────────────┐
│ N+1 LAB 系统     │
├─────────────────┤
│ 📊 数据看板      │
│ 📦 订单管理      │
│ 🏪 产品中心      │
│ 🤝 经销商管理    │
└─────────────────┘
```

**折叠状态**：
```
┌────┐
│ N+1│
├────┤
│ 📊 │
│ 📦 │
│ 🏪 │
│ 🤝 │
└────┘
```

---

## 🔧 技术实现

### 1. CSS 样式

**文件**: `frontend/index.html` (第 15-30 行)

```css
/* 侧边栏基础样式 */
.sidebar { 
    width: 220px; 
    transition: width 0.3s; 
}

/* 折叠状态 */
.sidebar.collapsed { 
    width: 64px; 
}

/* 导航项文字隐藏 */
.nav-text { 
    transition: opacity 0.3s; 
}
.sidebar.collapsed .nav-text { 
    display: none; 
}

/* 导航项居中 */
.sidebar.collapsed .nav-item { 
    padding: 14px 8px; 
    justify-content: center; 
}
```

**表格优化**：
```css
th { white-space: nowrap; }  /* 表头不换行 */
td { 
    max-width: 300px; 
    overflow: hidden; 
    text-overflow: ellipsis; 
}
```

---

### 2. HTML 结构

**侧边栏**：
```html
<div class="sidebar" id="sidebar">
    <div class="logo">
        <span id="logo-text">N+1 LAB 系统</span>
    </div>
    <div class="nav">
        <div class="nav-item">
            <span>📊</span>
            <span class="nav-text">数据看板</span>
        </div>
        <!-- 其他菜单项... -->
    </div>
</div>
```

**Header 折叠按钮**：
```html
<div class="header">
    <div style="display:flex; align-items:center; gap:16px;">
        <button onclick="toggleSidebar()" title="折叠/展开菜单">
            ☰
        </button>
        <h2 id="page-title">数据看板</h2>
    </div>
    <div>● 系统在线</div>
</div>
```

**操作列垂直布局**：
```html
<td style="text-align:center;">
    <div style="display:flex; flex-direction:column; gap:4px; align-items:center;">
        <button>BOM 详情</button>
        <button>✏️ 改价</button>
        <button>📦 库存</button>
    </div>
</td>
```

---

### 3. JavaScript 功能

**折叠切换函数**：
```javascript
function toggleSidebar() {
    const sidebar = document.getElementById('sidebar');
    sidebar.classList.toggle('collapsed');
    // 保存状态到 localStorage
    localStorage.setItem('sidebarCollapsed', 
        sidebar.classList.contains('collapsed'));
}
```

**恢复状态函数**：
```javascript
function restoreSidebarState() {
    const collapsed = localStorage.getItem('sidebarCollapsed') === 'true';
    if (collapsed) {
        document.getElementById('sidebar').classList.add('collapsed');
    }
}
```

**页面加载时调用**：
```javascript
restoreSidebarState();
loadStats();
```

---

## 📊 优化效果对比

### 表格宽度优化

**优化前**：
- 操作列宽度：~350px（三个按钮横向）
- 总表格宽度：~1800px
- 需要横向滚动 ❌

**优化后**：
- 操作列宽度：~100px（三个按钮垂直）
- 总表格宽度：~1500px
- 适配标准屏幕 ✅

### 侧边栏空间利用

**展开模式**（默认）：
- 适合桌面端，文字清晰
- 导航项一目了然

**折叠模式**：
- 节省 156px 水平空间
- 适合小屏幕或需要更多表格空间时
- 图标依然清晰可辨

---

## 🎯 使用方式

### 折叠/展开侧边栏

1. 点击 Header 左侧的 **☰** 按钮
2. 侧边栏平滑收缩/展开
3. 状态自动保存

### 刷新页面

- 自动恢复上次的折叠/展开状态
- 无需重新调整

### 表格浏览

- 按钮垂直排列，不占用额外宽度
- 产品名、分类等列内容自动截断
- 适合 1366×768 及以上分辨率

---

## 📱 响应式适配

### 桌面端（≥1440px）
- 侧边栏展开（默认）
- 表格完整显示
- 所有功能可用

### 笔记本（1366×768）
- 侧边栏可折叠以节省空间
- 表格内容优化显示
- 按钮垂直排列

### 平板（<1024px）
- 建议折叠侧边栏
- 表格可能需要横向滚动
- 操作按钮依然可用

---

## 🔗 相关文件

| 文件 | 修改内容 |
|------|---------|
| `frontend/index.html` | 侧边栏 HTML、CSS、JS 功能 |
| `frontend/index.html` | 操作列垂直布局 |
| `frontend/index.html` | 表格样式优化 |

---

## ✅ 测试验证

### 功能测试

1. ✅ 点击 ☰ 按钮 - 侧边栏折叠/展开
2. ✅ 刷新页面 - 状态保持
3. ✅ 切换菜单 - 标题正确显示
4. ✅ 操作按钮 - 三个按钮垂直排列
5. ✅ 表格宽度 - 适配页面宽度

### 浏览器测试

- ✅ Chrome 120+
- ✅ Firefox 120+
- ✅ Safari 17+
- ✅ Edge 120+

---

## 💡 后续优化建议

### 高优先级
1. 📱 **移动端适配** - 完全响应式布局
2. 🎨 **主题切换** - 深色/浅色主题

### 中优先级
3. 🔍 **表格列自定义** - 用户可选择显示哪些列
4. 📊 **固定表头** - 滚动时表头固定

### 低优先级
5. 🎯 **快捷键** - Ctrl+B 切换侧边栏
6. 🎨 **动画优化** - 更流畅的过渡效果

---

**优化完成**: 2026-04-04 10:55  
**测试状态**: ✅ 功能正常，样式正确  
**下一步**: 刷新浏览器查看效果（Ctrl+Shift+R）
