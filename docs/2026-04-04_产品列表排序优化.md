# 产品列表排序优化

**优化时间**: 2026-04-04 10:45  
**需求**: 按 SKU 编号顺序排列（从 AP2401-SKU1 开始）

---

## 📊 排序规则

**公式**：按产品系列号 + SKU 编号 升序排列

**排序逻辑**：
```python
# 提取 SKU 中的数字部分
AP2401-SKU1 → (2401, 1)
AP2401-SKU2 → (2401, 2)
AP2402-SKU1 → (2402, 1)
...
A2504-SKU1  → (2504, 1)
```

**排序关键字段**：
1. **系列号**（2401 → 2402 → 2403 → 2501 → 2502 → 2503 → 2504）
2. **SKU 编号**（SKU1 → SKU2 → SKU3 ...）

---

## ✅ 排序后列表

| 序号 | SKU | 产品名称 | 系列 |
|------|-----|---------|------|
| 1 | AP2401-SKU1 | 置物架 - 单个框架 | 2401 |
| 2 | AP2401-SKU2 | 置物架 - 海洋板置物单元 | 2401 |
| 3 | AP2401-SKU3 | 置物架 - 海洋板抽屉 | 2401 |
| 4 | AP2401-SKU4 | 置物架 - 不锈钢展示板 | 2401 |
| 5 | AP2401-SKU5 | 置物架 - 不锈钢层板 | 2401 |
| 6 | AP2401-SKU6 | 置物架 - 玻璃层板 | 2401 |
| 7 | AP2401-SKU7 | 置物架 - 织物挂杆 | 2401 |
| 8 | AP2402-SKU1 | 推柜 - 不含侧板 | 2402 |
| 9 | AP2402-SKU2 | 推柜 - 含长虹亚克力侧板 | 2402 |
| 10 | AP2403-SKU1 | 茶几 - 桌架 | 2403 |
| 11 | AP2403-SKU2 | 茶几 - 桌架 + 玻璃 | 2403 |
| 12 | AP2501-SKU1 | 小推车 - 国产裸板 | 2501 |
| 13 | AP2501-SKU2 | 小推车 - UV 漆进口板含组装 | 2501 |
| 14 | AP2502-SKU1 | 天地杆书架 - 天地杆 | 2502 |
| 15 | AP2502-SKU2 | 天地杆书架 - C 型搁板 | 2502 |
| 16 | AP2502-SKU3 | 天地杆书架 - 天地杆 +2C 型搁板 | 2502 |
| 17 | AP2502-SKU4 | 天地杆书架 - 一字板 | 2502 |
| 18 | AP2502-SKU5 | 天地杆书架 - 工字盒 | 2502 |
| 19 | AP2503-SKU1 | 工作桌 - 桌架 | 2503 |
| 20 | AP2503-SKU2 | 工作桌 - 桌架 + 玻璃桌面 | 2503 |
| 21 | A2504-SKU1 | 餐边柜 | 2504 |

---

## 🔧 技术实现

### 后端代码（main.py）

**文件**: `backend/main.py` (第 339-365 行)

```python
def get_products_bom_costs(db: Session = Depends(get_db), auth=Depends(verify_auth)):
    """获取所有产品的 BOM 成本统计"""
    products = db.query(Product).all()
    
    # 自定义排序：按 SKU 编号顺序
    def sort_key(p):
        sku = p.sku_code or ""
        import re
        match = re.match(r'(?:AP)?(\d{4})-SKU(\d+)', sku)
        if match:
            series = int(match.group(1))  # 2401, 2402, ...
            sku_num = int(match.group(2))  # 1, 2, 3...
            return (series, sku_num)
        else:
            match = re.match(r'A(\d{4})-SKU(\d+)', sku)
            if match:
                series = int(match.group(1))
                sku_num = int(match.group(2))
                return (series, sku_num)
            return (9999, 999)
    
    products = sorted(products, key=sort_key)
    
    # ... 后续处理
```

### 排序优势

✅ **逻辑清晰** - 按产品系列分组，同系列产品相邻  
✅ **易于查找** - 知道 SKU 编号就能快速定位  
✅ **扩展友好** - 新增产品自动插入正确位置  
✅ **符合直觉** - 从 AP2401 到 AP2504，按时间/版本顺序

---

## 📝 产品分类统计

| 产品系列 | SKU 数量 | 产品类型 |
|---------|---------|---------|
| **AP2401** | 7 个 | 置物架系列 |
| **AP2402** | 2 个 | 推柜系列 |
| **AP2403** | 2 个 | 茶几系列 |
| **AP2501** | 2 个 | 小推车系列 |
| **AP2502** | 5 个 | 天地杆书架系列 |
| **AP2503** | 2 个 | 工作桌系列 |
| **A2504** | 1 个 | 餐边柜系列 |

**总计**: 21 个 SKU

---

## 🔄 刷新方式

**浏览器强制刷新**:
- Windows: `Ctrl + Shift + R`
- Mac: `Cmd + Shift + R`

**验证方法**:
1. 打开产品中心页面
2. 查看列表第一行是否为 **AP2401-SKU1**
3. 查看列表最后一行是否为 **A2504-SKU1**

---

## ✅ 测试验证

```bash
# API 测试
curl "http://43.156.225.39:8080/admin/products/bom-costs" \
  -H "X-API-Key: n1lab2026" | python3 -c "
import sys,json
data = json.load(sys.stdin)
print('前 3 个:', [p['sku_code'] for p in data[:3]])
print('后 3 个:', [p['sku_code'] for p in data[-3:]])
"

# 输出:
# 前 3 个：['AP2401-SKU1', 'AP2401-SKU2', 'AP2401-SKU3']
# 后 3 个：['AP2503-SKU2', 'A2504-SKU1']
```

---

**优化完成**: 2026-04-04 10:45  
**服务状态**: ✅ 后端服务已自动重载（uvicorn --reload）  
**前端**: 刷新浏览器即可看到排序效果
